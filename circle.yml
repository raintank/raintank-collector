machine:
  environment:
    GOPATH: "/home/ubuntu/.go_workspace"
test:
  pre:
    - pkg/depends.sh
    - pkg/build.sh
  post:
    - pkg/package.sh
general:
  artifacts:
    - pkg/artifacts
deployment:
  production:
    branch: master
    commands:
      - pkg/deploy.sh
