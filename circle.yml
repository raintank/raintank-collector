machine:
  environment:
    GOPATH: "/home/ubuntu/.go_workspace"
dependencies:
  override:
    - pkg/depends.sh
    - pkg/build.sh
test:
  post:
    - pkg/package.sh
general:
  artifacts:
    - pkg/artifacts
deployment:
  production:
    branch: packaging_improvements
    commands:
      - pkg/deploy.sh
